{
  "$comment": "Read only by dependency-cruiser, so it resolves the web app's @/* alias. Without the paths mapping every `@/...` import resolves to nothing and the module boundary rules check an empty graph — a green tick that means nothing.",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "composite": false,
    "declaration": false,
    "declarationMap": false,
    "jsx": "preserve",
    "baseUrl": ".",
    "paths": { "@/*": ["apps/web/src/*"] }
  }
}
